name: Monitor CAPUFE / GN_Carreteras

on:
  schedule:
    # Corre cada 10 minutos. GitHub puede retrasar el disparo unos minutos
    # en horas de mucha carga (limitación normal de Actions, no del script).
    - cron: "*/10 * * * *"
  workflow_dispatch: {}  # permite correrlo manualmente desde la pestaña Actions

permissions:
  contents: write  # necesario para que el workflow pueda guardar los resultados

jobs:
  monitorear:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar el repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: pip install requests pandas openpyxl

      - name: Ejecutar monitor
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python monitor_capufe_gn.py

      - name: Guardar resultados en el repositorio
        run: |
          git config user.name "monitor-bot"
          git config user.email "monitor-bot@users.noreply.github.com"
          git add eventos_mexico_puebla.xlsx tweets_vistos.json monitor_log.txt docs/data/eventos.json || true
          git diff --staged --quiet || git commit -m "Actualizar eventos detectados"
          git push
